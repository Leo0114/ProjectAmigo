---
import { rotaryOne, rotaryTwo, rotarySad } from "@data/Rotary.json";
---

<div class="max-w-[1400px] mx-auto px-4">
  <h2 class="prose-lg dark:text-slate-300 text-slate-800">
    Los clubes rotarios que actualmente (últimos cinco años) apoyan el Proyecto
    Amigo:
  </h2>

  <div class="flex flex-wrap p-4 transition-all duration-300">
    <div class="w-full lg:w-1/3">
      <ul class="list-disc list-inside dark:text-slate-300 text-slate-800">
        {rotaryOne.map((one) => <li>{one.name} </li>)}
      </ul>
    </div>

    <div class="w-full lg:w-1/3">
      <ul class="list-disc list-inside dark:text-slate-300 text-slate-800">
        {rotaryTwo.map((two) => <li>{two.name} </li>)}
      </ul>
    </div>

    <div class="w-full lg:w-1/3">
      <ul class="list-disc list-inside dark:text-slate-300 text-slate-800">
        {rotarySad.map((sad) => <li>{sad.name} </li>)}
      </ul>
    </div>
  </div>
</div>
